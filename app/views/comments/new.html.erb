<h1>Commentを作成する</h1>

<%= form_for([@review, @comment]) do |f| %>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content ,autofocus: true %>
  </div>
  <div class="field">
    <%= f.submit '送信する' %>
  </div>
<% end %>
